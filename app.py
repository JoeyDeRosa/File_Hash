# outline for file hasher

def run_app
  input =
  folders = [input]
  if input == folder:
    for i in folders:
      if i == file:
        if hash == True:
          hash(file)
        else:
          un_hash(file):
      if i == folder:
        folders.append(i)
  else:
    if hash == True:
      hash(file)
    else:
      un_hash(file)

def hash
  for i in file:
    i = hash_key

def un_hash
  for i in file:
    i = un_hash_key
